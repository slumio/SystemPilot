[package]
name = "syspilot-collector"
version = "0.1.0"
edition = "2021"
description = "Bounded, priority-aware SysPilot collector handoff"
license = "MIT"
repository = "https://github.com/slumio/SystemPilot"
homepage = "https://github.com/slumio/SystemPilot"

[dependencies]
syspilot-abi = { version = "0.1.0", path = "../syspilot-abi" }
