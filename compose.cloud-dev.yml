services:
  postgres:
    image: postgres:16-bookworm
    environment:
      POSTGRES_DB: syspilot
      POSTGRES_PASSWORD: ${SYSPILOT_DEV_ADMIN_PASSWORD:?set SYSPILOT_DEV_ADMIN_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d syspilot"]
      interval: 2s
      timeout: 2s
      retries: 20
    volumes:
      - cloud-postgres:/var/lib/postgresql/data
    security_opt:
      - no-new-privileges:true

  migrate:
    image: postgres:16-bookworm
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      FLEET_DATABASE_URL: postgresql://postgres:${SYSPILOT_DEV_ADMIN_PASSWORD:?set SYSPILOT_DEV_ADMIN_PASSWORD}@postgres:5432/syspilot?sslmode=disable
      FLEET_DB_ALLOW_INSECURE: "1"
      PGPASSWORD: ${SYSPILOT_DEV_ADMIN_PASSWORD:?set SYSPILOT_DEV_ADMIN_PASSWORD}
      SYSPILOT_DEV_RUNTIME_PASSWORD: ${SYSPILOT_DEV_RUNTIME_PASSWORD:?set SYSPILOT_DEV_RUNTIME_PASSWORD}
      SYSPILOT_DEV_PEPPER: ${SYSPILOT_DEV_PEPPER:?set SYSPILOT_DEV_PEPPER}
      SYSPILOT_DEV_TOKEN: ${SYSPILOT_DEV_TOKEN:?set SYSPILOT_DEV_TOKEN}
    volumes:
      - ./deploy/fleet:/deploy/fleet:ro
    entrypoint: ["/deploy/fleet/dev-bootstrap.sh"]
    security_opt:
      - no-new-privileges:true

  collector:
    build:
      context: .
      dockerfile: Dockerfile.cloud
    depends_on:
      migrate:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: postgresql://syspilot_runtime:${SYSPILOT_DEV_RUNTIME_PASSWORD:?set SYSPILOT_DEV_RUNTIME_PASSWORD}@postgres:5432/syspilot?sslmode=disable
      SYSPILOT_CREDENTIAL_PEPPER: ${SYSPILOT_DEV_PEPPER:?set SYSPILOT_DEV_PEPPER}
      LISTEN_ADDR: 0.0.0.0:8080
      DATABASE_CONNECTIONS: 16
      CONCURRENT_REQUESTS: 256
      RUST_LOG: syspilot_cloud=info
    ports:
      - 127.0.0.1:8080:8080
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    mem_limit: 512m
    cpus: 2.0

volumes:
  cloud-postgres:
